<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{subject}}</title>
  <style>
    /* Base styles */
    body {
      margin: 0;
      padding: 0;
      font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      background-color: #f9f9f9;
      -webkit-font-smoothing: antialiased;
    }
    
    /* Container */
    .email-container {
      max-width: 600px;
      margin: 0 auto;
      background-color: #ffffff;
    }
    
    /* Header */
    .header {
      padding: 20px;
      text-align: center;
      background-color: #f8f4ef;
      border-bottom: 3px solid #e6d9c9;
    }
    
    .logo {
      max-width: 180px;
      height: auto;
    }
    
    /* Content */
    .content {
      padding: 30px;
      background-color: #ffffff;
    }
    
    /* Hero section */
    .hero {
      position: relative;
      margin-bottom: 30px;
    }
    
    .hero-image {
      width: 100%;
      height: auto;
      border-radius: 8px;
    }
    
    .hero-overlay {
      position: absolute;
      bottom: 0;
      left: 0;
      right: 0;
      background: linear-gradient(to top, rgba(0,0,0,0.7) 0%, rgba(0,0,0,0) 100%);
      padding: 20px;
      border-radius: 0 0 8px 8px;
    }
    
    .hero-title {
      color: #ffffff;
      font-size: 24px;
      margin: 0;
      font-weight: 600;
    }
    
    /* Typography */
    h1 {
      color: #4a4a4a;
      font-size: 24px;
      margin-top: 0;
      margin-bottom: 20px;
      font-weight: 600;
    }
    
    h2 {
      color: #4a4a4a;
      font-size: 20px;
      margin-top: 30px;
      margin-bottom: 15px;
      font-weight: 600;
    }
    
    p {
      margin-top: 0;
      margin-bottom: 20px;
      color: #666;
    }
    
    /* Buttons */
    .button {
      display: inline-block;
      padding: 12px 24px;
      background-color: #c2a87d;
      color: #ffffff !important;
      text-decoration: none;
      border-radius: 4px;
      font-weight: 600;
      text-align: center;
      transition: background-color 0.3s ease;
    }
    
    .button:hover {
      background-color: #b09a6f;
    }
    
    .button-container {
      margin: 30px 0;
      text-align: center;
    }
    
    /* Tables */
    .table {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 30px;
    }
    
    .table th {
      background-color: #f8f4ef;
      padding: 12px;
      text-align: left;
      font-weight: 600;
      color: #4a4a4a;
      border-bottom: 2px solid #e6d9c9;
    }
    
    .table td {
      padding: 12px;
      border-bottom: 1px solid #e6e6e6;
      color: #666;
    }
    
    .table tr:last-child td {
      border-bottom: none;
    }
    
    /* Summary box */
    .summary-box {
      background-color: #f8f4ef;
      border-radius: 8px;
      padding: 20px;
      margin-bottom: 30px;
    }
    
    .summary-row {
      display: flex;
      justify-content: space-between;
      margin-bottom: 10px;
    }
    
    .summary-row:last-child {
      margin-bottom: 0;
      padding-top: 10px;
      border-top: 1px solid #e6d9c9;
      font-weight: 600;
    }
    
    /* Footer */
    .footer {
      padding: 30px;
      text-align: center;
      background-color: #f8f4ef;
      color: #888;
      font-size: 14px;
      border-top: 3px solid #e6d9c9;
    }
    
    .social-links {
      margin-bottom: 20px;
    }
    
    .social-link {
      display: inline-block;
      margin: 0 10px;
    }
    
    .social-icon {
      width: 32px;
      height: 32px;
    }
    
    .footer-links {
      margin-bottom: 20px;
    }
    
    .footer-link {
      color: #888;
      text-decoration: underline;
      margin: 0 10px;
    }
    
    .footer-address {
      margin-bottom: 20px;
    }
    
    /* Divider */
    .divider {
      height: 1px;
      background-color: #e6e6e6;
      margin: 30px 0;
    }
    
    /* Product card */
    .product-card {
      display: flex;
      margin-bottom: 20px;
      border: 1px solid #e6e6e6;
      border-radius: 8px;
      overflow: hidden;
    }
    
    .product-image {
      width: 100px;
      height: 100px;
      object-fit: cover;
    }
    
    .product-details {
      padding: 15px;
      flex-grow: 1;
    }
    
    .product-name {
      margin: 0 0 5px 0;
      font-weight: 600;
      color: #4a4a4a;
    }
    
    .product-price {
      margin: 0;
      color: #888;
    }
    
    /* Responsive */
    @media screen and (max-width: 600px) {
      .content {
        padding: 20px;
      }
      
      .product-card {
        flex-direction: column;
      }
      
      .product-image {
        width: 100%;
        height: 150px;
      }
      
      .table {
        font-size: 14px;
      }
      
      .table th, .table td {
        padding: 8px;
      }
      
      .hero-title {
        font-size: 20px;
      }
    }
    
    /* Animation (for clients that support it) */
    @keyframes fadeIn {
      from { opacity: 0; }
      to { opacity: 1; }
    }
    
    .animate-fade-in {
      animation: fadeIn 0.5s ease-in-out;
    }
    
    @keyframes slideInUp {
      from { transform: translateY(20px); opacity: 0; }
      to { transform: translateY(0); opacity: 1; }
    }
    
    .animate-slide-in {
      animation: slideInUp 0.5s ease-in-out;
    }
  </style>
</head>
<body>
  <div class="email-container">
    <div class="header">
      <img src="{{shopLogo}}" alt="{{shopName}}" class="logo">
    </div>
    
    <div class="content">
      {{> @partial-block }}
    </div>
    
    <div class="footer">
      <div class="social-links">
        <a href="{{socialLinks.facebook}}" class="social-link" target="_blank">
          <img src="{{siteUrl}}/images/facebook-icon.png" alt="Facebook" class="social-icon">
        </a>
        <a href="{{socialLinks.instagram}}" class="social-link" target="_blank">
          <img src="{{siteUrl}}/images/instagram-icon.png" alt="Instagram" class="social-icon">
        </a>
        <a href="{{socialLinks.twitter}}" class="social-link" target="_blank">
          <img src="{{siteUrl}}/images/twitter-icon.png" alt="Twitter" class="social-icon">
        </a>
        <a href="{{socialLinks.pinterest}}" class="social-link" target="_blank">
          <img src="{{siteUrl}}/images/pinterest-icon.png" alt="Pinterest" class="social-icon">
        </a>
      </div>
      
      <div class="footer-links">
        <a href="{{siteUrl}}/shop" class="footer-link">Shop</a>
        <a href="{{siteUrl}}/about" class="footer-link">About Us</a>
        <a href="{{siteUrl}}/contact" class="footer-link">Contact</a>
        <a href="{{siteUrl}}/privacy" class="footer-link">Privacy Policy</a>
      </div>
      
      <div class="footer-address">
        {{shopAddress}}<br>
        {{shopPhone}} | {{shopEmail}}
      </div>
      
      <p>&copy; {{currentYear}} {{shopName}}. All rights reserved.</p>
    </div>
  </div>
</body>
</html>